<p style="color: green"><%= notice %></p>

<h1>Courses</h1>

<div id="courses">
  <% @courses.each do |course| %>
    <%= render course %>
    <p>
    </p>
  <% end %>
</div>

<%= link_to "New course", new_course_path %>
